import { SettingsCode } from ".";
export type DropdownValue = {
  Name: string;
  Value: any;
};

// Статические значения для Dropdown будут храниться тут (в случае изменения enum, достаточно будет обновить лишь здесь)
